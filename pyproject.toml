[project]
name = "coremusic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cython>=3.1.4",
    "setuptools>=80.9.0",
]

[dependency-groups]
dev = []
